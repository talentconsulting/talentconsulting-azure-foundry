name: Deploy .NET Version PR Creator

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-repo-metadata-agent.reusable.yml
    with:
      project_dir: agents/hosted/repometadata/repo-metadata-pr-creator
      service_name: repo-metadata-pr-creator
      expected_marker: invalid_repository
    secrets: inherit
