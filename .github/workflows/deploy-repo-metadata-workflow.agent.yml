name: Deploy .NET Version Workflow

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-repo-metadata-agent.reusable.yml
    with:
      project_dir: agents/hosted/repometadata/repo-metadata-workflow
      service_name: repo-metadata-workflow
      expected_marker: invalid_source_url
    secrets: inherit
