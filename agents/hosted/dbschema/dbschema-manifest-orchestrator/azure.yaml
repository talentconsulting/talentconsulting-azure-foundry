# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

requiredVersions:
  extensions:
    azure.ai.agents: '>=1.0.0-beta.4'

name: dbschema-manifest-orchestrator

services:
  dbschema-manifest-orchestrator:
    project: src/dbschema-manifest-orchestrator
    host: azure.ai.agent
    language: python
    uses:
      - ai-project
    codeConfiguration:
      dependencyResolution: remote_build
      entryPoint: main.py
      runtime: python_3_13
    container:
      resources:
        cpu: "0.5"
        memory: 1Gi
    description: Checks a GitHub database-schema manifest for changed repositories and creates one schema and manifest update pull request.
    environmentVariables:
      - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
        value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
      - name: DBSCHEMA_WORKFLOW_AGENT_NAME
        value: ${DBSCHEMA_WORKFLOW_AGENT_NAME}
      - name: DBSCHEMA_PR_CREATOR_AGENT_NAME
        value: ${DBSCHEMA_PR_CREATOR_AGENT_NAME}
      - name: DBSCHEMA_MANIFEST_MAX_ENTRIES
        value: ${DBSCHEMA_MANIFEST_MAX_ENTRIES}
      - name: DBSCHEMA_MANIFEST_MAX_SCHEMAS
        value: ${DBSCHEMA_MANIFEST_MAX_SCHEMAS}
    kind: hosted
    name: dbschema-manifest-orchestrator
    displayName: Database Schema Manifest Orchestrator
    protocols:
      - protocol: responses
        version: 2.0.0

  ai-project:
    host: azure.ai.project
    endpoint: ${AZURE_AI_PROJECT_ENDPOINT}

infra:
  provider: microsoft.foundry
