# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

requiredVersions:
  extensions:
    azure.ai.agents: '>=1.0.0-beta.4'

name: service-dependency-pr-creator

services:
  service-dependency-pr-creator:
    project: src/service-dependency-pr-creator
    host: azure.ai.agent
    language: python
    uses: [ai-project]
    codeConfiguration:
      dependencyResolution: remote_build
      entryPoint: main.py
      runtime: python_3_13
    container:
      resources: { cpu: "0.5", memory: 1Gi }
    description: Publishes generated service-dependency catalogs and an optional shared manifest update in one GitHub pull request.
    environmentVariables:
      - name: GITHUB_PR_TOKEN
        value: ${{connections.openapi-pr-github.credentials.github_token}}
    kind: hosted
    name: service-dependency-pr-creator
    displayName: Service Dependency PR Creator
    protocols:
      - protocol: responses
        version: 2.0.0
  ai-project:
    host: azure.ai.project
    endpoint: ${AZURE_AI_PROJECT_ENDPOINT}

infra:
  provider: microsoft.foundry
