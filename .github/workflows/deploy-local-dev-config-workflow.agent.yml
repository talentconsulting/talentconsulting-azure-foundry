name: Deploy Local Dev Config Workflow

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-local-dev-config-agent.reusable.yml
    with:
      project_dir: agents/hosted/localdevconfig/local-dev-config-workflow
      service_name: local-dev-config-workflow
      expected_marker: invalid_source_url
    secrets: inherit
