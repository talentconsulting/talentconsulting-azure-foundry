name: Deploy OpenAPI Source Discovery

on:
  push:
    paths:
      - "agents/hosted/openapi/openapi-source-discovery/**"
      - ".github/workflows/deploy-openapi-source-discovery.agent.yml"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: openapi-source-discovery-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy-and-test:
    if: github.event_name != 'push' || github.ref_name == github.event.repository.default_branch
    runs-on: ubuntu-latest
    environment: dev

    env:
      AGENT_PROJECT_DIR: agents/hosted/openapi/openapi-source-discovery
      AZD_ENV_NAME: ci
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_AI_PROJECT_ENDPOINT: ${{ secrets.AZURE_AI_PROJECT_ENDPOINT }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION || 'uksouth' }}
      AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP || 'talent-day-rg' }}
      AZURE_AI_ACCOUNT_NAME: ${{ vars.AZURE_AI_ACCOUNT_NAME || 'talent-day-foundry' }}
      AZURE_AI_PROJECT_NAME: ${{ vars.AZURE_AI_PROJECT_NAME || 'talent-day-proj-default' }}
      AGENT_TEST_SOURCE_URL: ${{ vars.OPENAPI_SOURCE_DISCOVERY_TEST_URL || 'https://github.com/talentconsulting/talentsuite-bidmanager/tree/main/src/TalentSuite.Server/Health' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate deployment configuration
        shell: bash
        env:
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: |
          set -euo pipefail
          missing=0
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID AZURE_AI_PROJECT_ENDPOINT; do
            if [ -z "${!name}" ]; then
              echo "::error::Missing dev environment secret $name."
              missing=1
            fi
          done
          exit "$missing"

      - name: Install Azure Developer CLI
        uses: Azure/setup-azd@v2

      - name: Install Foundry extensions
        working-directory: ${{ env.AGENT_PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          azd extension install azure.ai.agents
          azd extension install azure.ai.projects
          azd extension install microsoft.foundry

      - name: Sign in to azd with GitHub federated credentials
        shell: bash
        run: |
          set -euo pipefail
          azd auth login \
            --client-id "$AZURE_CLIENT_ID" \
            --federated-credential-provider "github" \
            --tenant-id "$AZURE_TENANT_ID"

      - name: Configure existing Foundry project
        working-directory: ${{ env.AGENT_PROJECT_DIR }}
        shell: bash
        env:
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: |
          set -euo pipefail
          project_id="/subscriptions/$AZURE_SUBSCRIPTION_ID/resourceGroups/$AZURE_RESOURCE_GROUP/providers/Microsoft.CognitiveServices/accounts/$AZURE_AI_ACCOUNT_NAME/projects/$AZURE_AI_PROJECT_NAME"

          azd env select "$AZD_ENV_NAME" || azd env new "$AZD_ENV_NAME" --no-prompt
          azd env set AZURE_SUBSCRIPTION_ID "$AZURE_SUBSCRIPTION_ID"
          azd env set AZURE_TENANT_ID "$AZURE_TENANT_ID"
          azd env set AZURE_LOCATION "$AZURE_LOCATION"
          azd env set AZURE_RESOURCE_GROUP "$AZURE_RESOURCE_GROUP"
          azd env set AZURE_AI_ACCOUNT_NAME "$AZURE_AI_ACCOUNT_NAME"
          azd env set AZURE_AI_PROJECT_NAME "$AZURE_AI_PROJECT_NAME"
          azd env set AZURE_AI_PROJECT_ID "$project_id"
          azd env set AZURE_AI_PROJECT_ENDPOINT "$AZURE_AI_PROJECT_ENDPOINT"
          azd env set FOUNDRY_PROJECT_ENDPOINT "$AZURE_AI_PROJECT_ENDPOINT"
          azd env set USE_EXISTING_AI_PROJECT true
          azd env set ENABLE_HOSTED_AGENTS true
          azd env set ENABLE_CAPABILITY_HOST false

      - name: Run unit tests
        working-directory: ${{ env.AGENT_PROJECT_DIR }}
        run: python3 -m unittest discover -s src/openapi-source-discovery -p 'test_*.py'

      - name: Deploy OpenAPI Source Discovery
        working-directory: ${{ env.AGENT_PROJECT_DIR }}
        run: azd deploy openapi-source-discovery --no-prompt

      - name: Clear cached subscription ID before agent commands
        working-directory: ${{ env.AGENT_PROJECT_DIR }}
        # azd deploy needs AZURE_SUBSCRIPTION_ID, but leaving it set breaks azd ai agent
        # show/invoke below: they resolve their token via `azd auth token`, whose tenant
        # lookup for a discoverable subscription ID has no service-principal fast path
        # and fails for this identity. Clearing it (blank, not unset) makes that lookup
        # skip cleanly and fall back to the tenant already established by azd auth login.
        run: azd env set AZURE_SUBSCRIPTION_ID ""

      - name: Verify deployed agent
        working-directory: ${{ env.AGENT_PROJECT_DIR }}
        run: azd ai agent show openapi-source-discovery --output json --no-prompt

      - name: Smoke test deployed contract
        working-directory: ${{ env.AGENT_PROJECT_DIR }}
        shell: bash
        run: |
          set -euo pipefail
          payload="{\"sourceUrl\":\"$AGENT_TEST_SOURCE_URL\"}"
          azd ai agent invoke openapi-source-discovery "$payload" > "$RUNNER_TEMP/openapi-source-discovery-response.txt" 2>&1
          cat "$RUNNER_TEMP/openapi-source-discovery-response.txt"

          if ! grep -q '"apiFile"' "$RUNNER_TEMP/openapi-source-discovery-response.txt"; then
            echo "::error::The deployed agent response did not contain apiFile."
            exit 1
          fi

          if ! grep -q '"supportingFiles"' "$RUNNER_TEMP/openapi-source-discovery-response.txt"; then
            echo "::error::The deployed agent response did not contain supportingFiles."
            exit 1
          fi

          echo "OpenAPI Source Discovery smoke test passed."
