# Hosted Agent Project

This directory is an independent Microsoft Foundry hosted-agent azd project.

- Keep runtime code under `src/openapi-spec-workflow-hosted/`.
- Keep agent deployment configuration in `azure.yaml`.
- Run tests before deployment.
- Do not commit `.azure`, credentials, virtual environments, or generated output.
