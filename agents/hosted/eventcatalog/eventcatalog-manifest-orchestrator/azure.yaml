# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

requiredVersions:
  extensions:
    azure.ai.agents: '>=1.0.0-beta.4'

name: eventcatalog-manifest-orchestrator

services:
  eventcatalog-manifest-orchestrator:
    project: src/eventcatalog-manifest-orchestrator
    host: azure.ai.agent
    language: python
    uses:
      - ai-project
    codeConfiguration:
      dependencyResolution: remote_build
      entryPoint: main.py
      runtime: python_3_13
    container:
      resources:
        cpu: "0.5"
        memory: 1Gi
    description: Checks a GitHub event-and-command-catalog manifest for changed repositories and creates one catalog and manifest update pull request.
    environmentVariables:
      - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
        value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
      - name: EVENTCATALOG_WORKFLOW_AGENT_NAME
        value: ${EVENTCATALOG_WORKFLOW_AGENT_NAME}
      - name: EVENTCATALOG_PR_CREATOR_AGENT_NAME
        value: ${EVENTCATALOG_PR_CREATOR_AGENT_NAME}
      - name: EVENTCATALOG_MANIFEST_MAX_ENTRIES
        value: ${EVENTCATALOG_MANIFEST_MAX_ENTRIES}
      - name: EVENTCATALOG_MANIFEST_MAX_CATALOGS
        value: ${EVENTCATALOG_MANIFEST_MAX_CATALOGS}
      - name: GITHUB_READ_TOKEN
        value: ${{connections.openapi-pr-github.credentials.github_token}}
    kind: hosted
    name: eventcatalog-manifest-orchestrator
    displayName: Event and Command Catalog Manifest Orchestrator
    protocols:
      - protocol: responses
        version: 2.0.0

  ai-project:
    host: azure.ai.project
    endpoint: ${AZURE_AI_PROJECT_ENDPOINT}

infra:
  provider: microsoft.foundry
