# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

requiredVersions:
  extensions:
    azure.ai.agents: '>=1.0.0-beta.4'

name: system-summary-manifest-orchestrator

services:
  system-summary-manifest-orchestrator:
    project: src/system-summary-manifest-orchestrator
    host: azure.ai.agent
    language: python
    uses: [ai-project]
    codeConfiguration:
      dependencyResolution: remote_build
      entryPoint: main.py
      runtime: python_3_13
    container:
      resources: { cpu: "0.5", memory: 1Gi }
    description: Reads the service-catalogue manifest, summarizes every listed repository, and publishes one combined system-summaries file.
    environmentVariables:
      - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
        value: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
      - name: SYSTEM_SUMMARY_GENERATOR_AGENT_NAME
        value: ${SYSTEM_SUMMARY_GENERATOR_AGENT_NAME}
      - name: SYSTEM_SUMMARY_PR_CREATOR_AGENT_NAME
        value: ${SYSTEM_SUMMARY_PR_CREATOR_AGENT_NAME}
      - name: SYSTEM_SUMMARY_MANIFEST_MAX_ENTRIES
        value: ${SYSTEM_SUMMARY_MANIFEST_MAX_ENTRIES}
    kind: hosted
    name: system-summary-manifest-orchestrator
    displayName: System Summary Manifest Orchestrator
    protocols:
      - protocol: responses
        version: 2.0.0
  ai-project:
    host: azure.ai.project
    endpoint: ${AZURE_AI_PROJECT_ENDPOINT}

infra:
  provider: microsoft.foundry
