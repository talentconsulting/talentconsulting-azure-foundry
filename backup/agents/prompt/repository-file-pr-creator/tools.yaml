tools:
  - type: mcp
    server_label: GitHub
    server_url: https://api.githubcopilot.com/mcp
    require_approval: never
    project_connection_id: GitHub

tool_usage:
  github:
    purpose: "Create branches, commit structured file changes, and open pull requests."
    allowed_operations:
      - "Read repository contents"
      - "Read default branch metadata"
      - "Read file contents"
      - "Create branches"
      - "Create files"
      - "Update files"
      - "Commit file changes"
      - "Push multiple files to a branch"
      - "Create pull requests"
    disallowed_operations:
      - "Delete files"
      - "Delete branches"
      - "Merge pull requests"
      - "Approve pull requests"
      - "Modify repository settings"
      - "Trigger deployments"
      - "Write to repositories not present in the input"
