name: Deploy .NET Version Source Discovery

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-repo-metadata-agent.reusable.yml
    with:
      project_dir: agents/hosted/repometadata/repo-metadata-source-discovery
      service_name: repo-metadata-source-discovery
      expected_marker: repoMetadataFiles
    secrets: inherit
