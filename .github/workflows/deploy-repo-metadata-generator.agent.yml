name: Deploy .NET Version Generator

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-repo-metadata-agent.reusable.yml
    with:
      project_dir: agents/hosted/repometadata/repo-metadata-generator
      service_name: repo-metadata-generator
      expected_marker: invalid_input
    secrets: inherit
