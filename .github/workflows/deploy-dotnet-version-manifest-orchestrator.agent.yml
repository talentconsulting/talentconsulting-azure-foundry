name: Deploy .NET Version Manifest Orchestrator

on:
  push:
    paths:
      - "agents/hosted/dotnetversion/dotnet-version-manifest-orchestrator/**"
      - ".github/workflows/deploy-dotnet-version-manifest-orchestrator.agent.yml"
      - ".github/workflows/deploy-dotnet-version-agent.reusable.yml"
  workflow_dispatch:

jobs:
  deploy:
    if: github.event_name != 'push' || github.ref_name == github.event.repository.default_branch
    uses: ./.github/workflows/deploy-dotnet-version-agent.reusable.yml
    with:
      project_dir: agents/hosted/dotnetversion/dotnet-version-manifest-orchestrator
      service_name: dotnet-version-manifest-orchestrator
      expected_marker: invalid_input
    secrets: inherit
